namespace: monitoring
resources:
- manifests/clusterrole.yaml
- manifests/deployment.yaml
- manifests/service.yaml
- manifests/pvc.yaml
- manifests/alertmanager.yaml
# this for metrics
- kubeMetrics/cluster-role-binding.yaml
- kubeMetrics/cluster-role.yaml
- kubeMetrics/deployment.yaml
- kubeMetrics/service-account.yaml
- kubeMetrics/service.yaml

configMapGenerator:
- name: prometheus-server-conf
  files:
  - config-files/prometheus.rules
  - config-files/prometheus.yaml
generatorOptions:
  disableNameSuffixHash: true
